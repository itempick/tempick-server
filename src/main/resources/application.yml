spring:
  config:
    import:
      - application-security.yml

  datasource:
    url: jdbc:mysql://prod.cxy42iu4uqca.ap-northeast-2.rds.amazonaws.com:3306/prod?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: admin
    password: rladudtjr1!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    open-in-view: false


logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
